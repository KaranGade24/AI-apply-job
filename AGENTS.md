# Project Instructions

- **Naming Conventions**: All naming conventions across the project must be in `camelCase`. **Exception**: Model files should use `PascalCase` (e.g., `User`, `UserProfile`).
- **Language Requirements**: Write all files in JavaScript (`.js` or `.jsx`). Do NOT use TypeScript.
